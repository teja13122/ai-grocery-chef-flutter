# Copy this file to ".env" or pass the value via --dart-define.
#
# Run from the terminal with:
#   flutter run --dart-define=GEMINI_API_KEY=your_key_here
#
# Or use the VS Code "Run and Debug" panel (.vscode/launch.json already
# includes the key for you).
GEMINI_API_KEY=your_key_here
